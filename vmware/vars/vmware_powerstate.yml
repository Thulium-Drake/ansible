---
# Variables file for the VMWare powerstate tasks
#
supported_actions: [powered-off,powered-on,reboot-guest,restarted,shutdown-guest,suspended,present]
poweroff_actions: [powered-off,reboot-guest,restarted,shutdown-guest,suspended]
poweron_actions: [powered-on,present]
